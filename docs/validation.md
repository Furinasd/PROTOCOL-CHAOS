# Validation & Known Limits

## 文档交付快照 · 2026-09-12

游戏源码基线：`9e8645740638823913a3dd18915287f61138c479`。本次纳入项目文档与原先被忽略的 Arts 资源，未提交本地既存的 `EnemyHitbox.cs` / `MainScene.unity` 修订和实验测试。

| 层级 | 本次状态 | 证明范围 |
| --- | --- | --- |
| 源码 / Git / 场景静态检查 | 已检查 | 调用链、配置、资源引用、历史删除触发 |
| 文档链接 / 发布资源清单 | 交付时检查 | 文档导航与纳入范围，不证明运行行为 |
| Unity 编译 | 未执行 | 不声称当前源码编译通过 |
| EditMode / PlayMode 测试 | 未执行 | 本地实验测试不属于公开快照 |
| Editor Play Mode | 未执行 | 未复现当前四阶段流程 |
| Windows Player | 未执行 | 未制作或验证此次新构建 |
| 首次玩家 / 策略 / 性能实验 | 未执行 | 无新体验收益或性能数字 |

历史本地 `Codex-EditModeTests.log` 记录授权错误，不是通过报告。Unity Test Framework 依赖存在，也不等于有自动化覆盖。

静态资源扫描检查了 35 个已跟踪/待纳入的场景、Prefab、材质和 Asset 文件，并对照 Assets 与本机 PackageCache 的 13,730 个 GUID。12 个 GUID 未在该元数据集合中解析，涉及 PC_Renderer、DefaultVolumeProfile 和 MiSans SDF。此结果不是 Unity 导入结论；包版本、资源序列化残留或真实缺失需在 Editor 中区分，不能声称已通过全项目 GUID 完整性检查。Markdown 本地链接与封面 SVG XML 校验通过。已检查历史中的常见凭据模式未发现匹配，但该模式扫描不构成全面安全审计。

15 个纳入资产均有对应 `.meta`，本地 SHA-256 清单核对通过。新文档的 `git diff --check` 通过；原始 Unity YAML/Shader 含既有行尾空白，本次保留资源原文，没有为格式检查批量重写资产。未解析的 MiSans 项是生成设置中的历史 sourceFontFileGUID，其性质不同于运行时对象引用。

## 已知限制

| 项目 | 当前事实 | 下一验证 |
| --- | --- | --- |
| 第三方资源 | 音频与字体已按作者确认纳入；具体下载来源/授权文本尚待补齐，不适用项目 MIT 再授权 | 补齐逐项来源；Unity 检查资源导入 |
| Phase 2 | 提交基线初始架势 100；Hitbox 在受击端结算后又打满架势并反馈 | 单一结算、空初始架势、真实两次弹刀推进 |
| 污染区 | 净化无常规调用；没有找到寿命/数量上限，池可扩容 | 长战斗数量、空间覆盖、内存与回收 |
| 时间窗 | 规避与弹刀混用 scaled / unscaled；移动阈值按每帧位移 | 30/60/120 FPS、顿帧重叠、连续输入 |
| 教学结论 | 满能与处决计数不验证策略理解 | 无讲解首次玩家记录 |
| 表现装配 | arenaPlatform、部分 Boss 音效/终幕资源、Player visualRoot 未绑定 | 场景/Prefab 与分辨率检查 |
| 源码遗留 | HUD 兼容空实现、枚举桥、旧接口仍存在 | 不将调用存在写成完整能力 |

## 完整交付回归清单

- [ ] 从干净目录恢复工程，Unity 6000.3.6f1 编译无 Error，场景无 Missing Script/Prefab/资源。
- [ ] 清除 QuestFlow 存档，从 Phase 1 走到 Completed。
- [ ] 同色吸收、异色受伤、资源不足弹刀、成功弹刀、Dash/Jump 近失、普通处决与 Boss 两次处决。
- [ ] 每次攻击的能量、架势与反馈只结算预期次数；重复 Collider 不重复奖励。
- [ ] Phase 3 目标真实生成，不能依赖缺目标兜底跳过门槛。
- [ ] 死亡 R/按钮重开、鼠标锁定、Time.timeScale、再次启动存档行为。
- [ ] 污染区长时间生成：数量预算、回池、Layer 6、重叠与异常核心。
- [ ] 1920×1080 与一种非 16:9 分辨率下检查任务文本、能量与处决提示。
- [ ] 新 Windows Build Profile 构建冷启动完成流程，记录 SHA、硬件、日期与结果。
- [ ] 性能在固定压力场景记录帧时间、CPU/GPU 和 GC Alloc；不以编辑器瞬时 FPS 替代。

每次验证保存：源码 SHA → 场景与参数 → 操作步骤 → 实际结果 → 日志/录像 → 未覆盖条件。日志与大构建包不直接混入源码目录。
